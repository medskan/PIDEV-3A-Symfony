{% extends 'base.html.twig' %}
{% block title %}
    <h1>Ajout Catégorie</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ asset('base-back.html.twig')}}">Home</a></li>
            <li class="breadcrumb-item active">Ajout categorie</li>
        </ol>
    </nav>
{% endblock %}
{%  block body %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <table>
        <tr>  <td> {{ form_label(form.nomC) }}</td>
            <td> {{ form_widget(form.nomC) }}</td>
            <td>{{ form_errors(form.nomC) }}</td>
        </tr>

        <tr>  <td>{{ form_label(form.type) }}</td>
            <td>  {{ form_widget(form.type) }}</td>
            <td>{{ form_errors(form.nomC) }}</td>

        </tr>


    </table>
    {{ form_end(form) }}

{% endblock %}
