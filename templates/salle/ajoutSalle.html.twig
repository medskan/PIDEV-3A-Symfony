{% extends 'base.html.twig' %}

{% block title %}
    <h1>Ajout Salle</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ asset('base.html.twig')}}">Home</a></li>
            <li class="breadcrumb-item active">Ajout Salle</li>
        </ol>
    </nav>
{% endblock %}

{% block body %}

                                {# {{ form (f) }}#}
                                {{ form_start(f,{'attr':{'novalidate':'novalidate'}}) }}   <form>
                                    <table>
                                        <tr>
                                            <td> {{ form_label(f.id,"Identifiant de la salle ")}} </td>
                                            <td> {{ form_widget((f.id)) }} </td>
                                            <td> {{ form_errors((f.id)) }} </td>
                                        </tr>
                                        <tr>
                                            <td> {{ form_label(f.nomS,"Nom de la salle")}} </td>
                                            <td> {{ form_widget((f.nomS)) }} </td>
                                            <td> {{ form_errors((f.nomS)) }} </td>
                                        </tr>
                                        <tr>
                                            <td> {{ form_label(f.adresseS,"Adresse de la salle")}} </td>
                                            <td> {{ form_widget((f.adresseS)) }} </td>
                                            <td> {{ form_errors((f.adresseS)) }} </td>
                                        </tr>
                                        <tr>
                                            <td> {{ form_label(f.codePostal,"Code postal ")}} </td>
                                            <td> {{ form_widget((f.codePostal)) }} </td>
                                            <td> {{ form_errors((f.codePostal)) }} </td>
                                        </tr>
                                        <tr>
                                            <td> {{ form_label(f.ville,"Ville")}} </td>
                                            <td> {{ form_widget((f.ville)) }} </td>
                                            <td> {{ form_errors((f.ville)) }} </td>
                                        </tr>
                                        <tr>
                                            <td> {{ form_label(f.nombreP,"Nombre des employés")}} </td>
                                            <td> {{ form_widget((f.nombreP)) }} </td>
                                            <td> {{ form_errors((f.nombreP)) }} </td>
                                        </tr><tr>
                                            <td> {{ form_label(f.image,"Image")}} </td>
                                            <td> {{ form_widget((f.image)) }} </td>
                                            <td> {{ form_errors((f.image)) }} </td>
                                        </tr>

                                    </table>
    {{ form_end(f) }}
{% endblock %}