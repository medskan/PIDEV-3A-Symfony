{% extends 'front.html.twig' %}


{% block title %}Ajouter Commentaire{% endblock %}
{% block body %}
    <center>

    <section class="contact-section spad">
        <div class="container">
            <div class="row">
                <div class="col-lg-6">
                    <div class="card-body">
                        <div class="col-lg-6">
                            <div class="leave-comment">
                                {{ form_start(f,{'attr':{'novalidate':'novalidate'}}) }}

                                <form>
                                <table>

        <tr>
            <td>{{ form_label(f.redacteurCom,"Rédacteur") }}</td>
            <td> {{ form_widget(f.redacteurCom) }}</td>
            <td> {{ form_errors(f.redacteurCom) }}</td>
        <tr>

        <tr>
            <td>{{ form_label(f.text,"Contenu") }}</td>
            <td> {{ form_widget(f.text) }}</td>
            <td> {{ form_errors(f.text) }}</td>
        <tr>
        <tr>
            <td>{{ form_label(f.image,"Image") }}</td>
            <td> {{ form_widget(f.image) }}</td>
            <td> {{ form_errors(f.image) }}</td>
        <tr>
            {{ form_end(f) }}
    </table>
    </form>
    </div>
    </div>
    </div>
    </div>
    </div>
    </div>
    </section>
    <center>


{% endblock %}