{% extends 'front.html.twig' %}

{% block title %}Ajouter Abonné{% endblock %}
{% block body %}
    <div class="fox">
        <center>
            {{ form_start(f,{'attr':{'novalidate':'novalidate'}}) }}
            {{ form_widget(f) }}
            {{ form_end(f) }}
        </center>
    </div>
{% endblock %}