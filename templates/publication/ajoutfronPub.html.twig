{% extends 'front.html.twig' %}

{% block title %}Ajouter Publication{% endblock %}
{% block body %}
    <center>
        <section class="contact-section spad">
            <div class="container">
                <div class="row">
                    <div class="col-lg-6">
                        <div class="card-body">
                            <div class="col-lg-6">
                                <div class="leave-comment">
                                    {{ form_start(f,{'attr':{'novalidate':'novalidate'}}) }}
                                    <form>
                                        <table>
        <tr>
            <td>{{ form_label(f.redacteurPub,"Rédacteur") }}</td>
            <td> {{ form_widget(f.redacteurPub) }}</td>
            <td> {{ form_errors(f.redacteurPub) }}</td>
        <tr>
        <tr>
            <td>{{ form_label(f.contenu,"Contenu") }}</td>
            <td> {{ form_widget(f.contenu) }}</td>
            <td> {{ form_errors(f.contenu) }}</td>
        <tr>
        <tr>
            <td>{{ form_label(f.note) }}</td>
            <td> {{ form_widget(f.note) }}</td>
            <td> {{ form_errors(f.note) }}</td>

        <tr>
        <tr>
            <td>  <div class="rw-ui-container"></div></td>
        <tr>
        <tr>
            <td>{{ form_label(f.image,"Image") }}</td>
            <td> {{ form_widget(f.image) }}</td>
            <td> {{ form_errors(f.image) }}</td>
        <tr>


    <center>

    {{ form_end(f) }}
        </table>
        </form>
        </div>
        </div>
        </div>
        </div>
        </div>
        </div>
        </section>

    </center>
{% endblock %}